import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { JhiDataUtils } from 'ng-jhipster';

import { ILecturer } from 'app/shared/model/userinfo/lecturer.model';

@Component({
  selector: 'jhi-lecturer-detail',
  templateUrl: './lecturer-detail.component.html',
})
export class LecturerDetailComponent implements OnInit {
  lecturer: ILecturer | null = null;

  constructor(protected dataUtils: JhiDataUtils, protected activatedRoute: ActivatedRoute) {}

  ngOnInit(): void {
    this.activatedRoute.data.subscribe(({ lecturer }) => (this.lecturer = lecturer));
  }

  byteSize(base64String: string): string {
    return this.dataUtils.byteSize(base64String);
  }

  openFile(contentType = '', base64String: string): void {
    this.dataUtils.openFile(contentType, base64String);
  }

  previousState(): void {
    window.history.back();
  }
}
