apply plugin: "war"

bootWar {
    mainClassName = "vn.vnedu.tgusurvey.surveystore.SurveystoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
